build:
  image: python
  environment:
    - TEST_KEY="$(cat ~/.ssh/id_rsa)"
  commands:
    - ls -la /
    - ls -la /drone
    - pwd
    - ls -la
    - echo $TEST_KEY

deploy:
  ssh:
    host: 188.166.218.1
    user: deploy
    port: 22
    commands:
      - ./deploy.sh
      - touch world
